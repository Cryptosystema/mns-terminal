# 🚨 PHASE 28 - VERCEL ENVIRONMENT SETUP

## ⚠️ КРИТИЧЕСКАЯ НАСТРОЙКА

**Проблема**: 3D код создан и запушен, но **НЕ работает на продакшене**  
**Причина**: На Vercel отсутствует переменная окружения `VITE_ENABLE_3D=true`

---

## ✅ ИСПРАВЛЕНИЕ (2 минуты)

### Шаг 1: Откройте Vercel Dashboard
```
https://vercel.com/cryptosystema/mns-terminal/settings/environment-variables
```

### Шаг 2: Добавьте переменную окружения
```
Name:  VITE_ENABLE_3D
Value: true
Environment: Production, Preview, Development (все 3 галочки)
```

### Шаг 3: Redeploy последний коммит
```bash
# В Vercel Dashboard → Deployments → Latest → "Redeploy"
# ИЛИ запустите новый деплой:
git commit --allow-empty -m "trigger: redeploy with VITE_ENABLE_3D"
git push origin main
```

### Шаг 4: Проверьте (через 1 минуту)
Откройте https://mns.com.ge и в консоли браузера должны появиться:
```
[Phase28] 🚀 initialize3DScene() called
[Phase28] 🔧 VITE_ENABLE_3D = true
[Phase28] ✅ Initializing 3D visualization...
[Phase28] ✅ 3D visualization initialized successfully!
```

---

## 🔍 DEBUG (если не работает)

### Проверка 1: Console.log в браузере
```javascript
// Откройте https://mns.com.ge
// Откройте DevTools (F12) → Console
// Должны увидеть логи [Phase28] 🚀

// Если НЕ видите логи Phase28:
console.log(document.getElementById('scene-3d-root'));
// Если null → билд старый, нужен redeploy
// Если <div> → контейнер есть, но JS не выполнился
```

### Проверка 2: Переменная окружения
```javascript
// Проверка что Vite подставил переменную:
// Откройте https://mns.com.ge/assets/index-*.js (в Sources)
// Найдите строку: import.meta.env.VITE_ENABLE_3D
// Должно быть подставлено: "true" (строка)
// Если undefined или false → переменная не установлена
```

### Проверка 3: React загружен
```javascript
// В Console проверьте:
window.React
window.ReactDOM
// Должны вернуть объекты, не undefined
```

---

## 📊 ЧТО ИЗМЕНИЛОСЬ В КОММИТАХ

### Коммит 33357d0 (Integration Fix)
- ✅ Добавлен `#scene-3d-root` контейнер в index.html
- ✅ Добавлена функция `initialize3DScene()` в main.ts
- ✅ React монтирование через `ReactDOM.createRoot()`

### Текущий коммит (Debug Logging)
- ✅ Добавлены подробные console.log с эмодзи
- ✅ Логи показывают каждый шаг инициализации
- ✅ Ошибки выводятся с full stack trace

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После настройки на https://mns.com.ge должна появиться:

**Черная секция 600px высотой** с:
- 🌀 Вращающийся 3D туннель (цилиндр)
- ✨ 1000 частиц (плавающие точки)
- 📈 Линии P10/P50/P90 (вероятностные поверхности)
- 🎨 Bloom эффекты (свечение)
- 🖱️ OrbitControls (вращение мышью)

**Расположение**: Между "Last updated: ..." и графиками Chart.js

---

## 🚀 БЫСТРЫЙ CHECKLIST

- [ ] Vercel environment variable: `VITE_ENABLE_3D=true` добавлена
- [ ] Redeploy triggered (коммит запушен или ручной redeploy)
- [ ] Подождали 1-2 минуты (время билда Vercel)
- [ ] Открыли https://mns.com.ge (Ctrl+Shift+R для hard refresh)
- [ ] Проверили Console → видим логи `[Phase28] 🚀`
- [ ] Визуально видим черную 3D секцию над графиками

---

**Если после этого НЕ работает** → напиши в chat, предоставь:
1. Screenshot консоли (полный вывод логов)
2. Screenshot страницы
3. Network tab → проверь размер bundle (должен быть ~1.3 MB)
